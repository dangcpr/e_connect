import 'package:flutter/material.dart';

class LoginTeacher extends StatelessWidget {
  const LoginTeacher ({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Đây là màn hình của Admin"),)
    );
  }
}